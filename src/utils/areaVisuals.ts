/** District imagery for service-area pages. */

export function areaImage(slug: string): string {
  return `/images/areas/${slug}.webp`;
}

export const AREAS_HERO_IMAGE = '/images/areas/srinagar.webp';
